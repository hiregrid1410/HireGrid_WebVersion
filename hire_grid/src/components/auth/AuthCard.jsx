import React from "react";

/**
 * AuthCard — The right-side card surface.
 *
 * Props:
 *   children {ReactNode}
 */
export default function AuthCard({ children }) {
  return <div className="auth-card">{children}</div>;
}
