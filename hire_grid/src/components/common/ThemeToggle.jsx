import React from "react";

// Theme toggle removed — application is permanently dark.
// Export kept so all existing imports remain valid without changes.
export function ThemeToggle() {
  return null;
}
